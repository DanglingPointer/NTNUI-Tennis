function openDialog(htmlstring,tittel) {
  var $dialog = $('<div></div>')
  .html(htmlstring)

  .dialog({
 	autoOpen: false,
	title: tittel,
	minWidth: 500,
	minHeight: 400
  });

  $dialog.dialog('open');
  // prevent the default action, e.g., following a link
  return false;
}
